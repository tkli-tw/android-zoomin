# AGENTS.md

## 專案意圖

Zoomin 是一個用來實驗與偶爾日常使用的小型 Android 放大鏡 App。這個專案的重點是享受開發與探索 AI 協作；請維持輕鬆、聚焦且容易調整，不要加入不必要的產品複雜度。

## 開始工作前

依序閱讀：

1. `doc/開發意向理解書.md`：理解專案為何存在，以及目前的人機共同理解。
2. `doc/交接說明.md`：掌握倉庫現況、待辦事項與尚未解決的問題。
3. 若需求仍然模糊，使用 `doc/開發意向理解建構流程.md` 協助理解，但不要把它變成僵硬的表單程序。

## 工作原則

- 開始實作前，先確認是否已知道「這個專案大致要做什麼」。這只需要足以辨認方向的模糊描述，不要求完整規格。
- 理解產品目的時，至少留意「為誰、在什麼情境、解決什麼問題、帶來什麼價值」。使用人群是目的的一部分，不能在未知時默認為所有人。
- 本專案目前只為開發者本人及已明確說出的使用情境設計。除非開發者主動擴大範圍，否則不要引入長者、低視力使用者或其他假想人群的需求與討論。
- 手機 App 的介面屬於產品意向的重要部分。進入 UI 實作前，依 `doc/開發意向理解建構流程.md` 的介面流程建立基本理解，並優先用線框、預覽或小型可操作成果確認。
- 不要把 AI 自行產出的第一版介面視為開發者已確認的設計；清楚標為提案，根據回饋更新 `doc/開發意向理解書.md`。
- 若專案目的在對話與文件中都不存在，可以用一個簡短問題詢問；得到答案後記入 `doc/開發意向理解書.md`，後續不要反覆詢問同一件事。
- 問詢過程中可隨時更新 `doc/開發意向理解書.md`。開發者未說且 AI 無法得知的資訊，應明確標成 `❓ 尚未理解`；不要把 AI 的建議寫成開發者已確認的意向。
- `❓ 尚未理解` 不等於阻塞事項。不要逐項盤問、反覆催答，或要求先清空所有問號才開始工作。
- 將改動維持在小型、容易理解且容易回復的範圍。
- 保留使用者的既有變更，不重寫與本次工作無關的程式或文件。
- 優先採用平台能力及專案既有慣例，避免不必要的相依套件。
- 除非使用者明確要求，否則不加入帳號、分析追蹤、廣告、網路功能或資料蒐集。
- 將相機畫面視為私人資料。除非功能明確需要且經使用者同意，否則不儲存或傳送影像。
- AI 應區分「使用者已表達的內容」、「為了前進而採用的推定」與「真正未知的事項」。
- 提問前先搜尋現有對話與文件。不要為了形式完整而詢問已經回答、可以查到，或能以低風險預設處理的事情。
- 低風險且容易修改的選擇，可以採用合理預設並繼續；會顯著改變產品方向、造成大量返工或產生外部影響的選擇，應先確認。
- 每次完成有意義的工作後，更新 `doc/交接說明.md`，並在 `doc/工作日誌.md` 追加成果紀錄。

## 產品基準

第一個可用版本暫定包含：

- 後鏡頭即時預覽
- 使用獨立的 `＋`、`−` 按鈕縮放；不使用縮放拉桿，其他縮放手勢待確認
- 使用獨立按鈕截圖，預設保存到系統圖片目錄
- 裝置支援時，使用獨立按鈕開啟或關閉相機手電筒以補光
- 裝置支援時啟用 CameraX 原生預覽穩定；自動保留最後穩定幀屬於已認可的實驗方向，實作前依意向理解書評估成本
- 清楚處理相機權限與不支援狀態
- 適合開發者本人實際使用的簡單操作
- 依直向／橫向及左手／右手模式調整工具列位置與按鈕起始方向，配置以 `doc/開發意向理解書.md` 為準

畫面暫停、雙指縮放、OCR、濾鏡、App 內相簿、雲端功能及營利功能暫不屬於基準範圍，除非後續探索對它們產生興趣。

這些項目是推動協作的暫定方向，不是不可更動的需求契約。

## Android 實作指引

- App 以 Flutter 與 Dart 實作，Android 為目前唯一目標平台。
- 介面使用 Flutter Material 3；只在套件無法提供必要能力時增加 Kotlin 原生程式碼。
- 相機使用 Flutter 官方 `camera` 套件，Android 端沿用其 CameraX 實作；只有明確需要更底層控制時才考慮原生 CameraX 或 Camera2。
- 主畫面使用 `CameraPreview`，截圖使用 `CameraController.takePicture()`；不自行建立 CPU 逐幀影像處理管線。
- 適度分離相機狀態與 UI 狀態，使部分行為不依賴實體相機也能測試。
- 執行時檢查裝置能力，不假設縮放、手電筒或特定鏡頭一定存在。
- 只申請目前功能真正需要的權限，並在介面中清楚處理拒絕及永久拒絕狀態。
- 使用者文字若開始增加多語需求，再集中到 Flutter 的在地化資源；原型階段允許少量中文字串。
- 優先設計直向操作，同時避免裝置設定變更造成崩潰或狀態損壞。

上述內容是 Android 專案建立前的預設方向。若實作限制需要採用不同方式，請將決定與原因記錄於 `doc/交接說明.md`。

## 品質檢查

依專案目前具備的能力，執行最相關的檢查，通常依序為：

1. 格式或靜態分析
2. 單元測試
3. Android Lint
4. Debug 組建

無法自動驗證的相機行為，應在 `doc/交接說明.md` 留下模擬器或實體裝置檢查項目。未實際驗證的裝置行為，不得宣稱已驗證。

## 文件分工

- `AGENTS.md`：長期有效的專案與 AI 協作規則；應留在根目錄，且不頻繁修改。
- `doc/開發意向理解書.md`：記錄專案動機、協作偏好與目前產品方向的動態理解。
- `doc/Android開發意向理解書範本.md`：可複製到其他 Android 專案的空白意向範本；它不是本專案已確認內容的來源。
- `doc/開發意向理解建構流程.md`：將模糊想法轉化成共同理解與小型實驗的可重複流程。
- `doc/交接說明.md`：目前狀態、決策、風險與確切下一步；更新時移除過時內容。
- `doc/工作日誌.md`：依時間追加有意義的完成成果；不作為待辦清單。

## 交接規範

結束一次有實質內容的工作前：

- 確認 `doc/交接說明.md` 符合倉庫的真實狀態。
- 記錄執行過的驗證及結果，也記下未能執行的檢查。
- 在 `doc/工作日誌.md` 增加有日期的成果摘要，不需要逐步記錄所有操作。
- 將未完成事項寫成 `doc/交接說明.md` 中清楚的下一步。
- 核心動機、方向、長期協作偏好、問詢所得理解或待理解事項有變化時，更新 `doc/開發意向理解書.md`。
